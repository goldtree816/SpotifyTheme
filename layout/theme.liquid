<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
  </head>
  <body>
    {% section 'navbar' %}
    {% section 'hero' %}
    {{ content_for_layout }}
    {% section 'footer' %}
  </body>
</html>
