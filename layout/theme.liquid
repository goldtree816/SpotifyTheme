<!DOCTYPE html>
<html lang="en" data-anim-enabled="{% if settings.enable_animations %}true{% else %}false{% endif %}" data-anim-intensity="{{ settings.animation_intensity | default: 60 }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ page_title }}</title>
  {{ content_for_header }}
  {{ 'global.css' | asset_url | stylesheet_tag }}
  {{ 'hero-3d-shoe.css' | asset_url | stylesheet_tag }}
  {{ 'hero-3d-shoe-config.js' | asset_url | script_tag }}
  {{ 'ux.js' | asset_url | script_tag }}
  {{ 'hero-3d-shoe.js' | asset_url | script_tag }}
  <style>
    body {
      overflow-x: hidden;
      margin: 0;
      font-family: sans-serif;
    }
    /* Firefox */
    html {
      scrollbar-width: thin;
      scrollbar-color: var(--color-primary, #0276d9) #1a1a1a;
    }
    /* Chrome, Edge, and Safari */
    body::-webkit-scrollbar {
      width: 12px;
    }
    body::-webkit-scrollbar-track {
      background: #1a1a1a;
    }
    body::-webkit-scrollbar-thumb {
      background-color: var(--color-primary, #0276d9);
      border-radius: 4px;
      border: 1px solid var(--color-primary, #0276d9);
      box-shadow: 0 0 5px var(--color-primary, #0276d9), 0 0 10px var(--color-primary, #0276d9);
    }
  </style>
  {{ 'cart-drawer.css' | asset_url | stylesheet_tag }}
  {{ 'cart-drawer.js' | asset_url | script_tag }}
</head>
<body>
  <div id="scroll-progress"></div>
  <main id="MainContent" role="main" tabindex="-1">
    {{ content_for_layout }}
  </main>
  <button id="backToTop" aria-label="Back to top" title="Back to top">▲</button>
</body>
</html> 